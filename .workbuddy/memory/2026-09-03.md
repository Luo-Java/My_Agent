# 2026-09-03 工作日志

## 会话级 RAG 开关（对话时选择资料库，不选则不用 RAG）
需求：对话时配合使用 RAG 要加开关——没选择搜索资料库就不检索。

**设计**：检索从「按智能体/全局自动检索」改为「会话级显式选库」（conversation.rag_kb_id，NULL=不检索）。前端输入区 📚 下拉即时写回（PUT /api/chat/conversation/{id}/rag-kb），发送链路零改动；规划模式每步、通用助手兜底均跟随会话开关。

**改动清单**：
- DB：conversation 加 rag_kb_id BIGINT NULL（schema.sql + alter.sql；**存量库已执行 ALTER**）
- 实体/DTO：Conversation + ragKbId；ConversationSummary/NewConversationResponse + ragKbId；新增 RagKbRequest(kbId)
- ConversationService.updateRagKb(conversationId, kbId) 单趟 UPDATE（null=关闭，不校验库存在性，删除后检索侧自动降级）
- KbService：buildKbContext(Agent,String) → **buildKbContext(Long kbId, String query)** 单库检索；TOP_K_AGENT/GLOBAL(4/4) 收敛为 TOP_K_RAG=6；kbId 空/库已删返回空串；类注释同步
- ChatComposer：buildRequest/buildDefaultRequest 从 conv.getRagKbId() 取值；buildKbContext 签名 Long
- PlannerRoundHandler.executeSteps：每步注入 conv.ragKbId（规划会话同样受开关控制）
- 前端：index.html input-foot 加「📚 资料库」胶囊下拉（选项=不检索+全部库，专属库带智能体名；选中高亮绿）+ header rag-badge；app.js ragKbId ref / ragKbOptions computed（孤儿库占位）/ currentRagKbName / onRagKbChange 即时保存；新建会话默认 null；selectConversation 回显会话已存值
- 文案同步：知识库管理页 sub、kb 卡片「全局库·所有对话通用」→「会话可选」、实体 javadoc

**验证**：node --check OK；mvn package -o BUILD SUCCESS（13:29，文案修正后 13:32 重打）；jar 抽查含 ragKbId/TOP_K_RAG/rag-kb 端点/前端下拉。

**收尾文案统一**（自查清除「所有对话通用/自动检索」旧话术，全部改为「会话可选」语义）：ConversationController 端点 javadoc 补 rag-kb 行；KnowledgeBase.agentId 字段注释、schema.sql kb 注释、index.html 侧边栏注释/详情头/新建弹窗、CreateKbRequest/KnowledgeBaseController javadoc 同步修正。

**行为变化提醒**：旧逻辑「绑 agent 自动查专属库+全局库」取消——现在必须显式在 📚 里选库才检索（含全局库）。老数据 conversation.rag_kb_id 全 NULL = 全部默认关闭 RAG。

## Chroma 迁移尝试 → 用户决定整体放弃（已回滚）
用户先提出「改为使用 Chroma 向量库，但 MySQL 也要留存记录」，经确认走官方 spring-ai-chroma-store 2.0.0 + MySQL 双写方案；随后改主意要求卸载 chromadb，最终选择**整体放弃 Chroma 迁移**，维持 MySQL 自存向量 + 内存余弦现状。

**回滚动作（已完成）**：
- 删除 `D:\software\chroma`（本机 python venv 环境，实际未装上 chromadb，仅 pip）
- pom.xml 移除 spring-ai-chroma-store 依赖（含注释块）
- 本地 Maven 仓库删除 spring-ai-chroma-store-2.0.0 及传递依赖 spring-ai-vector-store（确认无其它项目引用残留）
- 任务清单 #128-#132（chroma 迁移 5 项）全部标记 deleted
- mvn package -o BUILD SUCCESS（14:50，增量 6s）

**经验**：venv 路径必须用 Windows 风格 `D:/...`，Git Bash `/d/...` 不被 Windows python 识别；spring-ai 2.0 的 chroma 包路径为 `org.springframework.ai.chroma.vectorstore`（如再启用可参考）；pom 引入 store 模块本体而非 starter（starter 自动配置会在启动时连接 Chroma 挡应用）。

## Chroma 服务端状态更新（用户已自行装好）
用户告知「chroma 本地已经安装好了」，探测确认：**Chroma 0.5.23 在本机 127.0.0.1:8000 运行中**（PID 6700，v2 heartbeat 正常，default 库下无 collection，空服务零负担）。
经确认用户选择 **「暂不接入，只确认服务」**：代码侧维持 MySQL 自存向量不动，pom 不加依赖、任务不复建；Chroma 服务由用户自行管理，后续如需接入再启用。

## Chroma 迁移落地（正式接入：官方 spring-ai 模块 + MySQL 双写留存）
用户确认服务端就绪后重新启动迁移，本次**完整落地**：

**1. pom + 依赖**：加 `spring-ai-chroma-store` 2.0.0（模块本体非 starter；BOM 管版本）；`dependency:resolve` 走公司镜像拉取成功；本地仓库同时出现 spring-ai-vector-store 传递依赖。

**2. API 侦察（javap + sources 解压确认）**：包 `org.springframework.ai.chroma.vectorstore`；
- `ChromaApi.builder().baseUrl(url).build()`（无需手动 RestClient/JsonMapper）
- `ChromaVectorStore.builder(api, embeddingModel).collectionName(c).initializeSchema(true).initializeImmediately(true).build()`
- Builder 默认装配 `ChromaFilterExpressionConverter`；CreateCollectionRequest 默认 `hnsw:space=cosine` → distance 为余弦距离，相似度 = 1-distance，与 MySQL 侧 MIN_SCORE 口径一致
- `doAdd` 内部会用同一 EmbeddingModel **再次向量化文本**（双算成本，确定性输出与 MySQL 一致）；`requireCollectionId` 需先初始化（initializeImmediately）
- Document：`new Document(id, text, metadata)`；检索用 `SearchRequest.builder().query(q).topK(n).similarityThreshold(ms).filterExpression("kb_id == " + kbId).build()`；结果 `Document.getScore()` = 1-distance
- javap 在 Windows 上**不认 Git Bash `/d/...` 绝对路径**，须 cd 到 jar 目录用相对文件名

**3. 新增 `ChromaVectorStoreService`**（org.luo.service）：lazy volatile store + initFailed 标记（初始化失败本次运行降级 MySQL，重启恢复）；upsertChunks/deleteByChunkIds/search 全 try-catch 只 warn；chunk id(String) 作 Chroma 文档 id，metadata 带 kb_id(int)/source；BATCH=500 分批；yaml 顶层 `chroma.base-url` / `chroma.collection-name`（默认 http://127.0.0.1:8000 / kb_chunks）。

**4. KbService 接入**：注入 ChromaVectorStoreService；抽 insertChunks（MP insert 回填 id 供 upsert）/chunkIdsOf/chunkIdsOfKb helpers；registerFile（同名替换先清旧块 id）/rechunkFile/deleteFile/deleteKb/deleteChunk/doAddBlocks 全部 **MySQL 先写后同步 Chroma**（Chroma 失败仅 warn）；`search()` 改 **Chroma 优先（where kb_id + 余弦 TopK）→ 空/异常回退 MySQL 全量余弦**；类 javadoc 同步双写语义。

**5. Controller**：`POST /api/kb/chroma/sync`（body kbId 缺省=全部库，幂等回填，返回 {synced}）；端点清单 javadoc 已补。

**6. 验证**：mvn package -o **BUILD SUCCESS**（14:28，19s）；jar 含 ChromaVectorStoreService/KbService/KnowledgeBaseController；curl Chroma HTTP v2 冒烟全通过（create cosine collection → upsert → where kb_id==7 命中 2 条 / ==99 空 → delete by ids → count=0 → cleanup）——服务端语义与 chroma-store 模块调用方式一致。**注**：v2 upsert/query 等端点 URL 需带 `tenants/default_tenant/databases/default_database` 前缀（裸 /api/v2/collections 404）。未启动项目（约定：打包后不启动，用户 IDE 自验）。

**7. 终检修复孤儿残留（14:31，BUILD SUCCESS 33s）**：复查双写覆盖时发现 registerFile（同名替换）与 rechunkFile（重分片）MySQL 删旧块后**未同步删 Chroma 旧 id 文档**——新块拿新自增 id，旧 id 向量会成为孤儿残留（检索不命中但占空间）。修复：两处 MySQL 删旧后立即 `chromaStore.deleteByChunkIds(oldIds)` 再写新。全量核对 8 条链路覆盖完备：deleteKb/registerFile 替换/rechunkFile/deleteFile/doAddBlocks/deleteChunk 全部"MySQL 先删/写 + Chroma 同步"，syncToChroma 回填、search Chroma 优先回退 MySQL。

**8. README 同步（14:35）**：README 此前未覆盖 RAG 功能，最小补充三处：核心特性加「知识库 RAG（会话级开关）」条目（文件上传/分片/双写/降级/sync）；技术栈表加 Chroma 0.5.x + spring-ai-chroma-store 2.0.0 行；环境要求加 Chroma（可选，未启动自动降级）。任务清单 #128-#132 全部 completed。

## 会话 RAG 语义升级：纯开关 + 自动多库（先默认全局库，路由到 agent 再查其专属库）
需求：检索不能只看会话里存的单一 kbId——开关打开时应先查默认（全局通用）向量库；若本轮路由到 agent，再追加查 agent 的向量库。用户经 AskUserQuestion 拍板 **「纯开关 + 自动多库」**（否决另两个选项）。

**改动清单**：
- DB：conversation 的 `rag_kb_id` 列逻辑替换为 `rag_enabled` TINYINT(1) NOT NULL DEFAULT 0（旧列保留作历史、不再读写，存量 NULL 与 0 等价）；schema.sql 同步新列、alter.sql 追加兼容 ALTER；**存量库已执行 ALTER 并 SHOW COLUMNS 确认**（14:57，root@root@123 agent 库）
- 实体/DTO：Conversation 字段 ragKbId→ragEnabled(Boolean)；ConversationSummary/NewConversationResponse 尾字段同名改；新增 `RagEnabledRequest(enabled)`、删除 RagKbRequest
- ConversationService：updateRagKb→updateRagEnabled(conversationId, enabled) 单趟 UPDATE（Boolean.TRUE.equals）；Controller 端点 PUT `/api/chat/conversation/{id}/rag`
- KbService：`buildKbContext(Boolean ragEnabled, Agent agent, String query)`——开启后 `ragTargets(agent)`=全局库（agent_id IS NULL，只查不建）＋路由 agent 专属库（存在时）；各目标库分别 search()（Chroma 优先→MySQL 余弦回退）**带真实 score**；多库命中合并按 score 降序统一截断 TOP_K_RAG=6 注入 `[知识库资料]`；开关关/空查询/无命中=空串。私有 record Hit(kb, chunk, score)
- 注入点收敛（本体库匹配当轮 LLM 实际处理对象）：ChatComposer.buildRequest（conv+绑定 agent）/ buildDefaultRequest（agent=null）/ PlannerRoundHandler 每步传**当步步骤 agent**（composer.ragOn(conv) 判定开关）
- 前端：输入区「📚 下拉选库」整段删除，改 **iOS 滑块开关**（.rag-toggle，复用 planner-toggle 结构 + 绿色 #10b981）；app.js ragKbId ref→ragEnabled，onRagEnabledChange PUT /rag，会话切换回显；顶部 rag-badge 用 currentRagOn 判定

**验证**：两遍残留扫描（java: ragKbId/RagKbRequest/rag-kb 零残留；前端: ragKb* 仅注释自然提及）→ **mvn package -o BUILD SUCCESS（14:57:51，17s）**，jar 抽查含 RagEnabledRequest/KbService$Hit（三属性）/ChatComposer/PlannerRoundHandler/ConversationService；README 核心特性条目与 MEMORY.md 检索语义同步改写为「纯开关自动多库」。**未启动项目**（约定：打包后不启动，用户 IDE 重启自验——需重启后新列/新开关才生效）。

**清理历史残留 rag_kb_id（15:03，用户指示「不需要的要从 alter.sql 删除」）**：alter.sql 移除 rag_kb_id 的 ADD COLUMN 语句与旧注释（schema.sql 本就无此列，双轨一致）；存量库执行 `ALTER TABLE conversation DROP COLUMN rag_kb_id` 成功，SHOW COLUMNS 确认仅剩 rag_enabled；README/MEMORY 复核为已是最新纯开关语义，仅将 MEMORY.md 中「rag_kb_id 保留作历史」表述更新为「已废弃清理」。教训：RAG 显式选库方案当日即被纯开关替代，历史列无迁移价值，废弃即删保持 schema 干净。

## 智能规划开关补对称 PUT 接口（15:09）
用户质疑「RAG 开关有更新接口，planner 怎么没有」→ 查实：planner 持久化原本靠「发消息时 body.planner 顺带写回」（ChatService L182 updatePlanner），服务层 updatePlanner 早已存在但 Controller 无独立端点；RAG 是拨动即 PUT 的干净路径，两开关不对称（planner 拨动后不发消息就刷新会丢状态）。用户拍板补对称接口。

**改动**：
- 新增 DTO `PlannerEnabledRequest(Boolean enabled)`（org.luo.dto，仿 RagEnabledRequest）
- ConversationService 新增 `updatePlannerSwitch(id, Boolean enabled)`：null→false；开启时先查会话——不存在静默、已绑定智能体抛 `AiBusinessException(BAD_REQUEST, "绑定智能体的会话不支持规划模式…")`（planner 与 agentId 互斥防御，仅新端点走此校验，不动 ChatService 消息写回链路）
- ConversationController 新增 `PUT /api/chat/conversation/{id}/planner`（body `{enabled}`），端点清单 javadoc 补行
- 前端：index.html planner-toggle input 加 `@change="onPlannerChange"`；app.js 新增 onPlannerChange（仿 onRagEnabledChange，PUT /planner 成功后同步 conv.planner 供徽标/列表回显，失败回滚 planMode + alert）；return 导出加 onPlannerChange。发送消息 body 仍带 planner（幂等冗余，兼容）
- 无 SQL 变更（planner 列已存在）

**验证**：node --check OK；mvn package -o **BUILD SUCCESS（15:09:09，13s）**，jar 含 PlannerEnabledRequest.class；未启动项目（用户 IDE 重启自验）。行为对齐后：两开关均拨动即存，刷新/切会话回显一致。

**补互斥守卫到消息链路（15:10，自查发现的不对称）**：PUT /planner 已拒绑定 agent 会话，但消息链路 `ChatService.runRound`（L182）对绑定 agent 会话发 `planner:true` 消息仍无条件写回——绕过 UI 直调 chat/stream API 可造成 agentId+planner 并存的脏状态（侧边栏 agent 标记 v-else-if 优先显示导致 UI 形态错乱）。修复：写回条件加 `!(TRUE.equals(planner) && conv.getAgentId() != null)`——绑定 agent 会话的规划请求**本轮临时生效（selectHandler 仍按 body.planner 走规划）但不持久化**，会话保持普通/智能体形态；planner 会话切回普通（planner=false、无 agent）不受影响。javadoc 同步说明。mvn package -o BUILD SUCCESS（15:10:39，18s）。

**README 文档补齐（15:12）**：API 一览会话表补 `PUT /conversation/{id}/planner` 与 `PUT /conversation/{id}/rag`（rag 行系此前漏补）；核心特性动态规划条目补「🧭 开关拨动即写回、与 agentId 互斥」；数据库表 conversation 行补 `rag_enabled` 列。纯文档改动，无需重打包。


**多库检索合并单查（15:17，用户「不可以一次查询多个库吗」）**：原 `buildKbContext` 逐库 for 循环 `search(kb, ...)`，每库独立走 Chroma→MySQL 回退，代价 = query 被重复 embedding N 次（通义远程 API，真实成本/延迟）+ Chroma N 次往返，且「一库 Chroma 命中、一库回退 MySQL」时分数尺度不同源混排。改为 `searchAll(targets, query, topK)`：Chroma 一次查全部目标库（`ChromaVectorStoreService.search(Collection<Long> kbIds,...)`，filter 用 OR 等值拼接 `kb_id == n1 OR kb_id == n2`——与单等值同构避开数字 IN 转换盲区；单库退化为等值，行为不变），候选放大 库数×topK 由 `trimHits` 统一按分降序截断；整体失败/无命中才整体回退 MySQL（单条 `kb_id IN (...)` + `embedOne(query)` 只调一次）。**ChromaHit record 加 kbId 字段**（upsert 已写 metadata，检索时解析回溯归属库拼来源）。语义权衡：合并截断后理论上极端高分集中于单库时会挤掉另一库低分命中，与逐库截断行为一致、可接受；消除了跨后端分数混排。附带修正 TOP_K_RAG javadoc「（6）」→「（3）」与常量对齐。mvn package -o **BUILD SUCCESS（15:17:46，15s）**；未启动项目。**需用户重启后实测**：Chroma filter 的 OR 表达式若不受支持会走 catch 自动降级 MySQL（不报错、不阻断对话），届时再微调。

**治理轮（15:40-15:55，用户问「项目还有哪些地方需要优化」后多选推进三项）**：
1. **接口访问控制（#136）**：新增 `ApiKeyInterceptor` + `ApiSecurityConfig`，`app.api-key`（env `APP_API_KEY`）为空=本地放行、配置后 /api/** 需 `X-Api-Key`（或 `Authorization: Bearer`）常量时间比对，401 统一 `{code,message}`；OPTIONS/静态页不拦。application.yaml 补配置节与注释；README 加「3.1 部署安全」（含 API Key 兜底默认值泄露提示：默认 sk- 明文仅供本地，提交仓库前必须置空走 env）。BUILD SUCCESS 15:45。
2. **KbService 拆分（#137）**：836→622 行。检索全套（buildKbContext/ragTargets/searchAll/trimHits/kbNames/Hit + embedOne/parseVector/cosine/embedding）迁出到新 `KbSearchService`（注入 kbMapper/chunkMapper/chromaStore/embeddingProvider，全局库查询内嵌 globalKb() 避免循环依赖）；KbService 保留库管理/文件分块/embedAll/Chroma sync，javadoc 改「只写不读检索」。`ChatComposer` 字段/构造/透传改注入 KbSearchService（PlannerRoundHandler 走 composer 不受影响）。删除用锚点 python 脚本（准确率高），残留扫描零。BUILD SUCCESS 15:48。
3. **澄清状态核实（#138）**：调研结论——ParamFillingService 是**无状态 DB 重放**设计（getHistory 全量 + countClarifyStreak/clarifyScopedHistory 最近 12 条），MemoryMergeService 摘要**只排除出上下文不物理删 chat_message**，故澄清段始终可回放、无断裂风险（重启/多实例天然一致），**无需也不能再新增字段**（用户此前拍板约定）。落地：双向契约注释写入 ParamFillingService 与 MemoryMergeService 类 javadoc（未来若物理归档旧消息须同步重放逻辑）。纯注释未重打包。
